@model MembershipUserCollection
@{
    ViewBag.Title = "Listado de Usuarios";
}
<h2>
    Listado de Usuarios</h2>
<p>
    @Html.ActionLink("Crear Nuevo", "Register")
</p>
<table class="table">
    <tr>
        <th>
            Usuario
        </th>
        <th>
            Email
        </th>
        <th>
            Fecha de creación
        </th>
        <th>
            Último inicio de sesión
        </th>
@*        <th>
            Aprobado?
        </th>
        <th>
            Bloqueado?
        </th>*@
        <th>
            En linea?
        </th>
        <th>
        </th>
    </tr>
    @foreach (MembershipUser item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastLoginDate)
            </td>
 @*           <td>
                @Html.DisplayFor(modelItem => item.IsApproved)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsLockedOut)
            </td>
 *@           <td>
                @Html.DisplayFor(modelItem => item.IsOnline)
            </td>
            <td>
                @Html.ActionLink("Eliminar", "Delete", new { id = item.UserName }, new { @class = "delete" })
            </td>
        </tr>
    }
</table>
<div id="dialog-confirm" title="Eliminar registro" style="display: none;">
    <p>
        <span class="ui-icon ui-icon-alert" style="float: left; margin: 0 7px 20px 0;"></span>
        ¿Está seguro de eliminar este usuario?</p>
</div>
<script type="text/javascript">

    $(".delete").click(function (e) {
        e.preventDefault();
        var url = $(this).attr("href");
        $("#dialog-confirm").dialog({
            resizable: false,
            width: 350,
            height: 200,
            modal: true,
            buttons: {
                "Eliminar": function () {
                    window.top.location = url;
                    $(this).dialog("close");
                },
                "Cancelar": function () {
                    $(this).dialog("close");
                }
            }
        });

    })
</script>
