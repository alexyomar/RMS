@model RMS.Models.Banner
@{
    ViewBag.Title = "Create";
}

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<ul id="crumbs">
    <li><a href="@Url.Action("Index", "Banner")">Página Web</a></li>
    <li><a href="@Url.Action("Index", "Banner")">Banners</a></li>
    <li>Crear Banners</li>
</ul>
<table border="0" cellpadding="0" cellspacing="0" style="width: 100%; margin: 30px 0px;">
    <tr>
        <td valign="middle">
            <h1 style="margin: 0px; padding: 0px">Crear Banners</h1>
        </td>
        <td>
            <a href="@Url.Action("Index", "Banner")" class="button" style="float: right;">
                <img src="@Url.Content("~/Content/img/back.png")" />
                &nbsp;&nbsp;Volver</a>
        </td>
    </tr>
</table>

<h2 class="red">Nota Importante:</h2>
<p>
    Las imágenes deben poseer el siguiente tamaño:
</p>
<ul>
    <li><b>Banner Home:</b> 1000px de Ancho por 320px de Alto.</li>
    <li><b>Banner Temporada Destacada:</b> 380px de Ancho por 512px de Alto.</li>
    <li><b>Banner Interno:</b> 984px de Ancho por 160px de Alto.</li>

</ul>



@using (Html.BeginForm("Create", "Banner", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Banner</legend>

        @*<div class="editor-label">
            @Html.LabelFor(model => model.Active)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Featured)
            @Html.ValidationMessageFor(model => model.Featured)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Active)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Active, new { @checked = "checked" })
            @Html.ValidationMessageFor(model => model.Active)
        </div>
        *@
        <div class="editor-label">
            @Html.Label("Tipo de Banner")
        </div>
        <div class="editor-field">
            @Html.DropDownList("IdBannerType", String.Empty)
            @Html.ValidationMessageFor(model => model.IdBannerType)
        </div>

        <div class="editor-label">
            @Html.Label("Imágen (formato JPG):")
        </div>
        <div class="editor-field">
            <input type="file" name="imagen" id="imagen" />
        </div>
        <p>
            <input type="submit" value="Crear" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
